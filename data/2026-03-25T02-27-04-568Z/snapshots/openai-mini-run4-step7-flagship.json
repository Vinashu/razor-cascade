{
  "system": "You are a senior engineer writing concise implementation updates.",
  "prompt": "You are contributing to TaskForge, a TypeScript + Bun CLI used in the RazorCascade memory reliability study.\n\nExecution mode: cascade\nStandardized task: 7. Code snippet generator\nObjective: Generate useful starter code from a short description.\n\nContext from prior work:\nGoal: Add AI-assisted decomposition that generates validated, deterministic subtasks from a goal (using the structured summary) and optionally persists them without violating existing CLI/storage invariants.\nDecisions: Implement a pure decomposition module (src/decompose.ts) that transforms goal + Summary → typed Decomposition, separate from CLI/store side effects.; Keep storage schema version = 1 and extend Task optionally with subtasks (no implicit migration unless required).; Persist via single JSON file at .taskforge/tasks.json using atomic write (temp file in same dir + rename).; Minimal CLI parser preserved; add a decompose command/flag that supports print-only or optional persist, rejecting persistence for done tasks.; Summarizer boundary remains: flagship consumes structured Summary (goal/decisions/risks/invariants) before decomposition.\nRisks: Schema drift / backward-compatibility: extending tasks with subtasks may break hand-edited or older store files without a migration path.; Over-compression or lossy summarization could drop context the decomposition needs, reducing usefulness or causing incorrect subtasks.; Validation mismatch between AI-generated decompositions and the typed schema (empty titles, ordering, or format) may cause rejected outputs or failed persists.\nSnippets: const STORAGE_PATH = '.taskforge/tasks.json'; | export type Decomposition = { goal: string; subtasks: { title: string; description?: string; done: boolean }[] }; | if (task.status === 'done') throw new Error('cannot persist decomposition for completed task'); | async function saveStoreAtomic(path, data) { /* write temp in same dir then fs.rename(temp, path) */ } | cli.register({ name: 'decompose', usage: 'decompose <id|--goal> [--save]', handler: decomposeHandler })\nInvariants: schema version = 1; storage file = .taskforge/tasks.json; reject if `task.status === 'done'`; completion invariant rejecting `task.status === 'done'`; preserve schema version = 1 unless model change forces explicit migration; reject decomposition persistence for `task.status === 'done'` per existing invariant\n\nProduce a concise engineering update that includes:\n1. Goal\n2. Proposed implementation details\n3. Validation or testing\n4. Risks",
  "response": "1. Goal  \nAdd deterministic AI-assisted task decomposition that converts a goal + structured `Summary` into a validated `Decomposition`, with CLI support to print or optionally persist subtasks while preserving current storage/CLI invariants.\n\n2. Proposed implementation details  \n- Add a pure module at `src/decompose.ts`:\n  - Input: goal string + structured `Summary` (`goal/decisions/risks/invariants`)\n  - Output:\n    ```ts\n    export type Decomposition = {\n      goal: string;\n      subtasks: { title: string; description?: string; done: boolean }[];\n    };\n    ```\n  - Keep generation deterministic and validate:\n    - non-empty goal\n    - subtasks array exists\n    - each subtask has non-empty `title`\n    - normalize `done: false` on creation\n- Extend task model optionally with `subtasks` without changing schema version:\n  - keep `schemaVersion = 1`\n  - ensure older/hand-edited files still load when `subtasks` is absent\n- Add CLI entry:\n  ```ts\n  cli.register({ name: 'decompose', usage: 'decompose <id|--goal> [--save]', handler: decomposeHandler })\n  ```\n  - Support:\n    - `decompose <taskId>`: load task + summary, print decomposition\n    - `decompose --goal \"...“`: generate from ad hoc goal, print only\n    - `--save`: persist subtasks onto an existing task only\n  - Reject persistence when:\n    ```ts\n    if (task.status === 'done') throw new Error('cannot persist decomposition for completed task');\n    ```\n- Persist through existing single-store path only:\n  ```ts\n  const STORAGE_PATH = '.taskforge/tasks.json';\n  ```\n  - Use atomic writes:\n    ```ts\n    async function saveStoreAtomic(path, data) { /* write temp in same dir then fs.rename(temp, path) */ }\n    ```\n\n3. Validation or testing  \n- Unit tests for `src/decompose.ts`:\n  - deterministic output for same goal + summary\n  - rejects empty goal / empty subtask titles\n  - preserves ordering and sets `done: false`\n- Store compatibility tests:\n  - load version 1 files with and without `subtasks`\n  - persist updated task without schema migration\n- CLI tests:\n  - print-only decomposition from task id\n  - print-only decomposition from `--goal`\n  - `--save` writes subtasks to `.taskforge/tasks.json`\n  - `--save` fails for `task.status === 'done'`\n- Atomic write test:\n  - temp-file-then-rename path used in same directory\n\n4. Risks  \n- Backward-compatibility risk from optional `subtasks` in version 1 store, especially with hand-edited JSON.\n- Summary quality risk: over-compressed summaries may omit context needed for useful decomposition.\n- Validation mismatch risk if generated subtasks violate schema expectations, causing rejected saves or degraded UX.",
  "usage": {
    "inputTokens": 530,
    "outputTokens": 631
  },
  "durationMs": 9558
}